# Roadmap

## Next up

- Add language-aware presets for JavaScript, Python, and docs-heavy repositories on top of the preset system.
- Publish example repositories that show what each preset produces.
- Add a label sync command for shipping a standard maintainer label set.
- Add a dry-run diff mode so maintainers can preview generated changes.
- Add a release-note schema option for teams that want structured outputs.
